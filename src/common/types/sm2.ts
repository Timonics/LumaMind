export type SMInput = {
  easeFactor: number;
  repetitions: number;
  interval: number;
};

export type SM2Result = {
  easeFactor: number;
  repetitions: number;
  interval: number;
  nextDueDate: Date;
};
